language: cpp
sudo: required
os: linux 
dist: trusty
cache: apt
script:
  - mkdir build
  - cd build
  - cmake .. && make && make install
  
compiler: gcc
addons: 
  apt: 
    sources: 
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
env: 
  - COMPILER=g++-5
  - CXX=g++-5
  